# detect_cam 实施规划

## 1. 目标与首版边界

在 RK3588 Android 设备本地完成三种商品的视觉录入、SKU 识别、拿起判断、连续跟踪与信息展示。

首版假设：相机固定、桌面固定、三种商品各一件、同时重点处理一件被拿起的商品；另外两件继续检测跟踪。单个 RGB 相机斜向俯拍，兼顾桌面和商品抬起后的区域。完全遮挡、离开画面不能保证实时定位；此时显示暂时丢失，不绘制伪装为实际观测的框。

如要求严格区分桌面平移与垂直抬起，或要求完全遮挡时仍可靠定位，需评估深度相机/第二视角；单目版本输出基于视觉证据的拿起推断。

## 2. 已有资料及可复用内容

实际目录为 `SKU_info/`，大小写必须保持一致。

| ID | 商品 | 信息来源 |
| --- | --- | --- |
| sku1 | 乐事原味薯片 | SKU_info/sku1_info.txt |
| sku2 | 可口可乐 248mL | SKU_info/sku2_info.txt |
| sku3 | 芙丝天然矿泉水 500mL | SKU_info/sku3_info.txt |

统一使用 sku1、sku2、sku3，将需求中的 sk2 视作 sku2。

父目录 smart_cam 已有 CameraCapture、NativeRknn、InferenceEngine、SkuLibrary、检测覆盖层和 ProductStateTracker。按模块审查后迁入独立工程，采用独立应用包名，避免覆盖原应用。

已有五类检测路径直接使用类别标签，并不加载特征模型；旧特征检索路径也不能直接视作已满足三种商品的新需求。已有模型需用这三种实物重测。已有拿放状态机仅依赖检测框位移，作为原型参考，不作为真实拿起判据。

## 3. 用户流程

### 商品录入

1. 选择 sku1 / sku2 / sku3，并显示对应资料，避免录错绑定。
2. 预览中央显示可调录入框，初始约占预览宽度 45%、高度 60%；这是视觉引导区域，须适配横竖屏。
3. 每次仅将一件商品放入框中，商品完整可见并留约 10% 边缘。优先用区域内商品检测框裁剪；不能把整个中央框的桌面背景直接当作商品特征。
4. 检查单目标、边缘截断、模糊、曝光和大小；检查通过后自动采集。引导缓慢转动，覆盖正面、侧面、背面、倾斜和手持状态。
5. 每件以 20–40 张有效且有差异的图片为初始目标；连续重复帧不计数，具体数量按识别验证调整。
6. 提取归一化特征，保存多视角模板、SKU ID、模型版本、预处理版本；保留不同视角，不只存一个平均向量。
7. 单独用未参与录入的新画面验证三类互认和未知物拒识，未通过时补录或调整模型。

中央框坐标必须正确映射到分析图像，处理预览裁剪、旋转、镜像、letterbox 与缩放。录入和识别必须使用相同模型、颜色通道、归一化和裁剪规则。

### 运行展示

完成三件录入后退出录入模式，隐藏中央框。三件放到桌面，系统检测整个有效视野，稳定识别后建立初始在位状态。可手动指定桌面范围及初始展位，首版用于缩小事件判断范围，不把展位编号当 SKU 身份。

每个物体显示跟踪框、SKU 和状态。确认拿起时展示对应 txt 的资料卡；通过 SKU ID 精确查找，不从带置信度的显示字符串解析身份。长文可滚动，缺失文件显示明确占位提示。

短时遮挡保留卡片和 SKU，标注“暂时遮挡”；恢复后验证身份再继续关联。放回桌面并稳定后提示已放回，再清除当前推荐卡。放到新位置需重新建立该轨迹的静置基准，避免只允许放回原槽位。

## 4. 技术流水线

相机帧 → 商品检测 → 多目标关联/跟踪 → 按需裁剪和特征提取 → SKU 稳定决策 → 拿放状态机 → 框与信息卡。

手部检测作为并行辅助分支，输出手的位置及其与商品的时序关系。

- 商品检测：优先评估轻量 YOLO 检测器，输入以 640×640 为起点，输出所有商品的位置；必要时用实拍数据微调单类“商品”检测器。通用模型不保证能检出薯片包装或被手遮挡的瓶子。
- 特征识别：先评估已有 embedding 模型；若背面、透明瓶、反光或手遮挡条件下不稳定，采用适合商品实例区分的特征模型并进行数据适配。录入是推理和模板存储，不是现场训练模型。
- 匹配：每个 SKU 多模板相似度聚合；同时要求最佳分数超过阈值、领先第二名足够多，再经过多帧确认。阈值用独立采集数据标定，不能把余弦相似度直接当正确概率。不达标输出 unknown。
- 跟踪：用 ByteTrack 思路的检测关联作为候选，结合运动预测、框重叠和外观特征处理遮挡及交叉。track_id 是物体轨迹，sku_id 是商品类别，两者分离。新轨迹、低置信度、遮挡恢复时重新识别；已稳定轨迹降低特征提取频率。
- 身份更新：稳定轨迹锁定 SKU；少量相反预测不立即切换。只有持续、足够强的证据才允许改判。不得自动把运行时低置信度画面写回录入库。
- 手部线索：手靠近/重叠只表示接触候选，结合商品移动、手与商品协同移动、桌面区域和持续时间推断拿起；不依赖简单单帧重叠。

## 5. 状态设计

待识别 → 桌面静置 → 接触候选 → 拿起候选 → 已拿起 → 放回候选 → 桌面静置。

遮挡、丢失、相机断流分别记录，不能用“没检测到”直接触发拿起。拿起候选在证据撤销时回退；已拿起的状态在短遮挡时保留历史，但暂停新的拿放确认。长时丢失后标记不确定，重新出现需重识别和关联。

首轮可用静置约 0.5–1 秒、候选确认约 0.2–0.4 秒、放回稳定约 0.5–1 秒作为实验起点，最终由现场动作速度和帧率调参。位置阈值按物体尺寸归一化，并基于真实采集时间处理时序。

单目拿起是概率性推断。手持商品和桌面平移须作为不同测试类别，明确记录误报，必要时升级传感器方案。

## 6. Android / RK3588 工程

已确认设备系统为 Android 13，摄像头为 USB 摄像头。独立 Android 应用优先复用已在设备验证的 Camera2 采集及 Java/JNI 基础。

接入前先连接实际 USB 摄像头，检查 Camera2 是否枚举到外接相机，并验证可用分辨率、帧率、预览和分析帧输出。如果厂商 Camera HAL 已将 USB 摄像头暴露为系统相机，则走 Camera2；否则再检查摄像头 UVC 兼容性，通过 Android USB Host 授权及适配的 UVC 采集方案接入。不能仅凭 USB 接口就假定 Camera2 或 UVC 一定可用。

首轮以 1280×720、30 FPS 作为协商目标，实际以摄像头和板端支持为准；模型输入单独缩放。验证相机授权、USB 拔插重连、前后台切换、图像旋转及长时间采集。相机断开后停止拿放判定并显示断开状态，重连后重建在位基准。

ONNX → RKNN-Toolkit2 转换 → Android JNI → RKNN Runtime/NPU。先建立 FP16 精度基线，再评估 INT8；特征模型量化后须重新验证检索阈值与模板。模型、SDK、Runtime 和驱动的兼容性以实际板端验证为准。

建议模块：CameraSource、EnrollmentController、RknnDetector、FeatureExtractor、SkuRepository、MultiObjectTracker、PickupStateMachine、ProductInfoRepository、OverlayView。

应用 assets 打包商品 txt；模型和录入特征放应用可访问的专用目录。保存版本信息，模型或预处理变化时让旧模板失效并提示重新录入。重启保留录入数据，断流和切相机后重建临时在位基准。

性能策略：采集、推理和 UI 分线程；只处理最新待分析帧，丢弃过期帧。商品检测和手检测按测得预算调度，embedding 按轨迹需要触发。记录采集至显示完整延迟，包含裁剪、拷贝、NPU 等待、后处理、识别和 UI，不只统计 NPU 时间。

## 7. 分阶段实施与验收

| 阶段 | 交付与通过条件 |
| --- | --- |
| 1. 板端基础 | 独立 APK、预览、中央 ROI、坐标映射、三份信息正确读取；确认 Android、相机、驱动与 Runtime |
| 2. 录入识别 | 三 SKU 多视角采集、持久化、重启恢复、独立测试集分类和 unknown；模型不通过时先补数据 |
| 3. 检测跟踪 | 三物体同屏、手持遮挡、转动、交叉、短失踪后恢复；统计 ID 切换和错误身份继承 |
| 4. 拿放联动 | 手部线索与时序状态机、拿起展示、放回恢复；覆盖触碰和平移负例 |
| 5. 优化验收 | INT8 可选、线程与内存优化、持续运行及异常恢复，形成实测报告 |

首版建议验收目标（待在设备上验证，并非现成性能承诺）：

- 有效视野且包装可辨识条件下，独立测试 SKU 识别准确率 ≥95%，同时报告 unknown 比例，不能只统计接受预测的样本。
- 每件至少 30 次独立拿放测试；拿起事件 precision / recall 分别统计，目标均 ≥95%。加入不少于 50 次触碰、桌面平移、只伸手、遮挡不拿的负例。
- 预览目标 25–30 FPS，检测更新目标 ≥15 FPS；确认拿起后的卡片更新目标 ≤300ms，动作开始到展示的 P50/P95 另行统计，包含候选等待。
- 短遮挡恢复、三物体位置交换、手持旋转、快速移动、未知物、两件同时拿起、相机重连均需测试。两件同时拿起即使不属于首版保证范围，也要安全呈现，不能错误合并 SKU。
- 连续运行 30 分钟记录温度、帧率、延迟和内存，检查热降频及泄漏。

## 8. 开工前需确认的现场信息

已确认：Android 13、USB 摄像头、斜向拍摄桌面。

待确认：USB 摄像头型号、Camera2 可见性、支持分辨率/帧率、RKNN Runtime/驱动版本及实际安装角度；是否仅三种各一件、是否允许同时拿两件；需实拍核对当前三份资料与商品包装是否一致。

## 9. 官方技术参考

- Rockchip RKNN Model Zoo：支持模型、RK3588 与转换/部署示例：https://github.com/airockchip/rknn_model_zoo
- Rockchip Android YOLO 示例：https://github.com/airockchip/rknn_model_zoo/blob/main/examples/yolov5/README.md
- Android 图像分析及最新帧策略：https://developer.android.com/media/camera/camerax/analyze

以上资料说明部署与图像处理能力；本项目识别、跟踪和拿起准确率仍须使用现场数据验证。
